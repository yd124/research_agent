# Agent Run agent_20260406T044407Z

## Proposal

- Hypothesis: Adding volume_liquidity back to the best 4-group set (returns+trend+volatility+interaction) at alpha=1.0 while verifying it against the current best may reveal whether the full 5-group set at alpha=1 can be nudged higher by trying alpha=0.7, a moderate reduction in regularization not yet tested on the winning feature set.
- Alpha: 0.7
- Feature groups: returns, trend, volatility, interaction
- Notes: Best known run uses returns+trend+volatility+interaction at alpha=1.0 (IC=0.01465). Alpha sweeps at 0.3 (too low) and 1.5 (marginally worse) have been tried on this 4-group set. Alpha=0.7 is an untested point between 0.3 and 1.0 on the winning feature set — a single-dimension change to test if slightly less regularization on the 23-feature model improves rank IC without overfitting.

## Result

- Accepted by runner: False
- Validation Mean Rank IC: 0.014647
- Validation IC Sharpe: 0.087887
- Validation Top Minus Bottom: 0.002559

## Reflection

- Decision: reject
- Summary: The alpha=0.7 experiment on the 4-group set (returns+trend+volatility+interaction, 23 features) produced a validation mean rank IC of 0.014647, which is essentially identical to the current best run at alpha=1.0 (0.014648) and the alpha=1.5 run (0.014647). All three are within noise of each other (~0.00001 difference). IC Sharpe (0.08789) and top-minus-bottom (0.00256) are likewise indistinguishable from prior runs on this feature set. This confirms that the 4-group model is insensitive to alpha in the range 0.7–1.5; the regularization frontier is flat in this region. The run was correctly rejected as it does not improve over the current best.
- Next idea: Alpha sweeps on the 4-group set are exhausted and provide no lift. The next logical move is a feature-level change: try replacing or substituting individual features within the 4 groups rather than adding/removing whole groups. Specifically, test a 3-group set of returns+trend+volatility (dropping interaction) at alpha=1.0 has already been tried and showed IC=0.01337 — so interaction clearly contributes. Instead, try adding only one feature from the volume_liquidity group (e.g., rel_volume_20d alone) to the 4-group set to see if a single liquidity signal adds marginal IC without the noise of the full group. This is a finer-grained feature-level probe not yet attempted.
